# Handoff para continuidade no servidor — 2026-08-15

> **SUPERADO pelo estado de 2026-08-16.** Este documento preserva o snapshot
> histórico da chegada ao servidor. O estado autoritativo atual está em
> `AGENTS.md`, `CLAUDE.md` e `docs/internal/project-context.md`: HEAD `0e90cf0` igual a
> `origin/main`, TASK-086/076/087 concluídas e deploy local validado. O drift
> Alembic descrito abaixo foi resolvido pela TASK-086.

## Ambiente autoritativo

- repositório: `C:\app\AIShoppingAgent`;
- sistema operacional: Windows Server;
- branch local: `main`;
- commit do pacote funcional: `fd68939bcaac3d5926af6bc43eee7c05913f727c`;
- mensagem: `feat(telegram): consolida fluxos determinísticos e recuperação de senha`;
- `origin/main` não foi atualizado.

O clone auxiliar da estação não deve ser usado como fonte de verdade para
novos commits: ele está uma base atrás e mostrava novamente arquivos da
correção de identidade de produto já commitada no servidor.

## Pacote funcional concluído

- `/recuperar` escolhe `SET_PASSWORD` sem `UserCredential` e
  `RECOVER_PASSWORD` quando existe credencial;
- `/senha` não é comando público;
- senha usa formulário HTTPS, token hash-only single-use com TTL de 10 minutos
  e política de 8–128 caracteres com maiúscula, minúscula, número e símbolo;
- `/entrar` foi preservado;
- mensagens soltas não chamam IA;
- `/criar_missao` abre estado por usuário e somente a próxima descrição chama
  o interpretador;
- `/cancelar_missao`, seleções numéricas e confirmações são determinísticos;
- cancelamento desativa a agenda na mesma transação;
- aliases digitados com hífen são aceitos, enquanto o menu formal usa `_`.

Validação registrada: 263 testes focados; 1.158 aprovados e 1 ignorado na suíte
não-integração; cobertura 91,11%; Ruff aprovado; duas integrações do pacote
aprovadas em PostgreSQL 18.4 real pelo runner controlado.

## Estado de execução

Não houve push, rebuild ou deploy desse pacote. Os containers ativos
continuavam saudáveis e executando a imagem anterior
`aishoppingagent-app:local`. Alterar o checkout não altera o código dentro dos
containers já criados.

## Pendência imediata registrada

O `alembic check` do runner oficial falha antes do Pytest por drift das
constraints:

- `mission_command_values` em `mission_transitions`;
- `store_source_type_values` em `stores`;
- `user_role_values` em `users`.

O relatório completo está em `docs/database/alembic-check-issue.md`. A correção ainda
não foi iniciada e não possui autorização implícita para alterar produção.

## Regras para a próxima sessão

1. Trabalhar diretamente em `C:\app\AIShoppingAgent`.
2. Ler integralmente `AGENTS.md`, `CLAUDE.md` e os documentos obrigatórios.
3. Confirmar `git status`, branch e HEAD antes de qualquer alteração.
4. Não usar o PostgreSQL ativo para diagnosticar migrations.
5. Reproduzir o drift em PostgreSQL 18.4 descartável.
6. Comparar migration, metadata SQLAlchemy e schema real antes de propor fix.
7. Não remover constraints nem desabilitar `alembic check` para obter sucesso.
8. Não criar nova TASK, fazer push, rebuild ou deploy sem solicitação explícita.
